version: "3.9"

services:
  # CLI 扫描器（一次性任务，挂载待扫描项目）
  aegis-scan:
    build:
      context: .
      target: cli
    volumes:
      - ${SCAN_TARGET:-./aegis-ai-core}:/target:ro
    command: ["/target", "--format", "json"]
    profiles:
      - scan
